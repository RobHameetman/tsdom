import AbortSignal from './AbortSignal';

describe('AbortSignal', () => {
	let abortSignal: AbortSignal | null = null;

	beforeAll(() => {
		/**
		 * ...
		 */
	});

	beforeEach(() => {
		/**
		 * ...
		 */
	});

	afterEach(() => {
		/**
		 * ...
		 */
	});

	afterAll(() => {
		/**
		 * ...
		 */
	});

	it.todo('should ...');
});
