import { jest } from '@jest/globals';
import { MutationRecord } from './MutationRecord';

describe('MutationRecord', () => {
	let range: MutationRecord | null = null;

	beforeAll(() => {
		/**
		 * ...
		 */
	});

	beforeEach(() => {
		/**
		 * ...
		 */
	});

	afterEach(() => {
		/**
		 * ...
		 */
	});

	afterAll(() => {
		/**
		 * ...
		 */
	});

	it.todo('should ...');
});
