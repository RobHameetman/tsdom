import { jest } from '@jest/globals';
import { MutationObserver } from './MutationObserver';

describe('MutationObserver', () => {
	let range: MutationObserver | null = null;

	beforeAll(() => {
		/**
		 * ...
		 */
	});

	beforeEach(() => {
		/**
		 * ...
		 */
	});

	afterEach(() => {
		/**
		 * ...
		 */
	});

	afterAll(() => {
		/**
		 * ...
		 */
	});

	it.todo('should ...');
});
